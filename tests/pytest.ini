[pytest]
markers =
    live: requires running infrastructure (pass --live)
addopts = -v --tb=short
testpaths = tests
filterwarnings =
    ignore:tagMap is deprecated.*:DeprecationWarning:ldap3.utils.asn1
    ignore:typeMap is deprecated.*:DeprecationWarning:ldap3.utils.asn1
